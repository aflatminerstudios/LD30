///scrSetSFXVolume(volume)

volume = argument0;

audio_sound_gain(sndEvilCollected,volume,0);
audio_sound_gain(sndGameOver1,volume,0);
audio_sound_gain(sndGameOver2,volume,0);
audio_sound_gain(sndGameOver3,volume,0);
audio_sound_gain(sndGameOver4,volume,0);
audio_sound_gain(sndGameOver5,volume,0);
audio_sound_gain(sndGameOver6,volume,0);
audio_sound_gain(sndGameOver7,volume,0);

audio_sound_gain(sndChatter1,volume,0);
audio_sound_gain(sndChatter2,volume,0);
audio_sound_gain(sndChatter3,volume,0);
audio_sound_gain(sndChatter4,volume,0);
audio_sound_gain(sndChatter5,volume,0);
audio_sound_gain(sndChatter6,volume,0);

audio_sound_gain(sndGoodCollected,volume,0);

audio_sound_gain(sndPowerup1,volume,0);
audio_sound_gain(sndPowerup2,volume,0);
audio_sound_gain(sndPowerup3,volume,0);
audio_sound_gain(sndPowerup4,volume,0);
audio_sound_gain(sndPowerup5,volume,0);